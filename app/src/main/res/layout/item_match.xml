<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="4dp"
    android:layout_marginBottom="4dp"
    android:background="@color/white"
    app:cardElevation="2dp"
    app:cardCornerRadius="4dp">

    <!-- Se elimina el ConstraintLayout anidado, ahora hay uno solo -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <!-- 1. Franja de la hora a la izquierda -->
        <TextView
            android:id="@+id/tv_match_time_vertical"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@color/colorAccent"
            android:gravity="center"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:rotation="270"
            android:textColor="@android:color/black"
            android:textSize="10sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="18:30" />

        <!-- 2. Escudo Local -->
        <ImageView
            android:id="@+id/iv_local_shield"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/tv_match_time_vertical"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@tools:sample/avatars" />

        <!-- 3. Contenedor del resultado en el centro -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cv_result_container"
            android:layout_width="60dp"
            android:layout_height="wrap_content"
            app:cardBackgroundColor="@color/blue_brand"
            app:cardCornerRadius="4dp"
            app:cardElevation="4dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/iv_visitor_shield"
            app:layout_constraintStart_toEndOf="@+id/iv_local_shield"
            app:layout_constraintTop_toTopOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingTop="4dp"
                android:paddingBottom="4dp">

                <!-- Texto del marcador -->
                <TextView
                    android:id="@+id/tv_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    tools:text="10 - 1" />

                <!-- "Ver Acta" invisible -->
                <TextView
                    android:id="@+id/tv_view_match_report"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Ver Acta"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:visibility="gone" />
            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <!-- 4. Nombre Equipo Local -->
        <TextView
            android:id="@+id/tv_local_team"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:gravity="end"
            android:maxLines="2"
            android:textColor="@color/black"
            android:textSize="10sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/cv_result_container"
            app:layout_constraintStart_toEndOf="@id/iv_local_shield"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="EQUIPO LOCAL CON UN NOMBRE MUY LARGO" />

        <!-- 5. Escudo Visitante -->
        <ImageView
            android:id="@+id/iv_visitor_shield"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/dummy"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@tools:sample/avatars" />

        <!-- 6. Nombre Equipo Visitante -->
        <TextView
            android:id="@+id/tv_visitor_team"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:gravity="start"
            android:maxLines="2"
            android:textColor="@color/black"
            android:textSize="10sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/iv_visitor_shield"
            app:layout_constraintStart_toEndOf="@id/cv_result_container"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="EQUIPO VISITANTE CON UN NOMBRE MUY LARGO" />

        <!-- 7. 'dummy' TextView como espaciador a la DERECHA -->
        <TextView
            android:id="@+id/dummy"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:gravity="center"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:rotation="270"
            android:textColor="@android:color/black"
            android:textSize="10sp"
            android:textStyle="bold"
            android:visibility="visible"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="     " />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
