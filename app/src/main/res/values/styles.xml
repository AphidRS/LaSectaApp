<resources>
    <!-- ... tus otros estilos ... -->

    <style name="ClassificationTableCell">
        <!-- El ancho ahora es 0dp para que se estire entre las guías -->
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="ClassificationTableHeader" parent="ClassificationTableCell">
        <item name="android:textStyle">bold</item>
        <!-- paddingStart y paddingEnd para que no se pegue a los bordes de la celda -->
        <item name="android:paddingStart">2dp</item>
        <item name="android:paddingEnd">2dp</item>
    </style>

</resources>
