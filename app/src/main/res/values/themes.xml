<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema Base para TODA la aplicación -->
    <style name="Theme.LaSectaApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Color primario (barra de estado, etc.) -->
        <item name="colorPrimary">@color/blue_brand</item>
        <item name="colorPrimaryVariant">@color/blue_brand</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Color de fondo para componentes como la BottomNavigationView -->
        <!-- ESTA LÍNEA SOLUCIONA EL FONDO NEGRO -->
        <item name="android:colorBackground">@color/white</item>

        <!-- Color de la barra de estado (la de arriba del todo con la hora y batería) -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

    </style>
</resources>
